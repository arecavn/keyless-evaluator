# Vercel Python runtime requirements
# Keep in sync with pyproject.toml [project.dependencies]
# Note: playwright is NOT included here — chatgpt_web provider requires
# a full browser environment and won't work on Vercel serverless functions.
# Use gemini, openai, or anthropic providers in Vercel deployments.

openai>=1.0.0
google-generativeai>=0.8.0
anthropic>=0.40.0
rich>=13.0.0
typer>=0.12.0
python-dotenv>=1.0.0
pydantic>=2.0.0
httpx[http2]>=0.27.0
jinja2>=3.1.0
anyio>=4.0.0
fastapi>=0.135.1
uvicorn>=0.41.0
